'use strict';

////////////// First Class Functions FCF ///////////////

// First class functions (FCF) means functions that are 'simply values'.
// - Functions are just another 'type of object'.
// - FCF can be stored in variables or properties.
// - We can pass FCF as arguments to other fiunctions.
// - FCF can be returned FROM functions
// - We can call methods on FCF (fnc.bind())

////////////// Higher Order Function HOF //////////////

// A function that reveives another function as an argument
// A function that returns a new function
// A Function that receives another function as an argument and that returns a new function.
